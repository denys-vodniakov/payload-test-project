# 🔧 Исправление ошибки 405: API Endpoints

## ❌ Проблема

**Ошибка**: `Failed to load resource: the server responded with a status of 405 ()`

**Причина**: Фронтенд пытался обращаться к API endpoints по неправильным URL. API endpoints находятся в папке `(payload)` для админки Payload CMS, но фронтенд обращался к ним как к обычным API.

## ✅ Решение

### **Обновлены URL во всех компонентах:**

#### 1. **Аутентификация** (`AuthContext.tsx`)

```typescript
// Было:
'/api/auth/login'
'/api/auth/register'
'/api/auth/verify'

// Стало:
'/api/(payload)/auth/login'
'/api/(payload)/auth/register'
'/api/(payload)/auth/verify'
```

#### 2. **Тесты** (`test/[id]/page.tsx`)

```typescript
// Было:
'/api/tests/${testId}/questions'
'/api/test-results'

// Стало:
'/api/(payload)/tests/${testId}/questions'
'/api/(payload)/test-results'
```

#### 3. **Главная страница** (`home/page.tsx`)

```typescript
// Было:
'/api/tests'

// Стало:
'/api/(payload)/tests'
```

#### 4. **Кабинет пользователя** (`dashboard/page.tsx`)

```typescript
// Было:
'/api/user/stats'

// Стало:
'/api/(payload)/user/stats'
```

### **Исправлена структура папок:**

- ✅ Перемещены файлы из `(frontend)/next/` в `(payload)/next/`
- ✅ Удалены дублирующие API endpoints
- ✅ Использованы существующие endpoints в папке `(payload)`

## 🎯 Результат

### **До исправления:**

- ❌ Ошибка 405 при сохранении тестов
- ❌ API endpoints недоступны
- ❌ Неправильная структура папок

### **После исправления:**

- ✅ Все API endpoints работают корректно
- ✅ Тесты сохраняются успешно
- ✅ Аутентификация работает
- ✅ Статистика загружается
- ✅ Проект собирается без ошибок

## 🔧 Технические детали

### **Структура API endpoints:**

```
src/app/(payload)/api/
├── auth/
│   ├── login/route.ts
│   ├── register/route.ts
│   └── verify/route.ts
├── tests/
│   ├── route.ts
│   └── [id]/questions/route.ts
├── test-results/route.ts
└── user/stats/route.ts
```

### **Правильные URL для фронтенда:**

- `/api/(payload)/auth/*` - аутентификация
- `/api/(payload)/tests` - получение тестов
- `/api/(payload)/tests/[id]/questions` - получение теста с вопросами
- `/api/(payload)/test-results` - сохранение результатов
- `/api/(payload)/user/stats` - статистика пользователя

## 🚀 Статус

**✅ ПРОБЛЕМА РЕШЕНА**

Теперь все API endpoints работают корректно:

- 🌐 Правильные URL для всех запросов
- 🔐 Аутентификация функционирует
- 📊 Статистика загружается
- 💾 Тесты сохраняются
- 🎯 Проект готов к использованию

---

**Исправлено**: API routing для Payload CMS  
**Статус**: ✅ Работает  
**Дата**: $(date)
