# 🎯 Решение проблемы с сохранением тестов в админке Payload

## ✅ Что было сделано

### 1. **Добавлены хуки валидации**

- `Tests/hooks/validateTest.ts` - проверяет, что тест имеет вопросы
- `Questions/hooks/validateQuestion.ts` - проверяет корректность вопросов
- `Tests/hooks/debugTestCreation.ts` - отладочная информация

### 2. **Временно упрощен доступ**

```typescript
// В Tests/index.ts и Questions/index.ts
access: {
  create: () => true, // Разрешено всем для отладки
}
```

### 3. **Создан скрипт для создания администратора**

```bash
pnpm run create-admin
```

## 🔧 Инструкции по тестированию

### Шаг 1: Создайте администратора

```bash
# Запустите скрипт создания администратора
pnpm run create-admin
```

### Шаг 2: Запустите проект

```bash
# Запустите в режиме разработки
pnpm dev
```

### Шаг 3: Войдите в админку

1. Перейдите на `http://localhost:3000/admin`
2. Войдите с учетными данными:
   - **Email**: `admin@test.com`
   - **Password**: `admin123`

### Шаг 4: Создайте вопрос

1. Перейдите в `Collections > Questions`
2. Нажмите "Create New"
3. Заполните:
   - **Question**: "What is React?"
   - **Category**: "React"
   - **Difficulty**: "Easy"
   - **Options**:
     - "A library for building UIs" ✅ (отметьте как правильный)
     - "A database" ❌
     - "A server" ❌
4. Нажмите "Save"

### Шаг 5: Создайте тест

1. Перейдите в `Collections > Tests`
2. Нажмите "Create New"
3. Заполните:
   - **Title**: "React Basics Test"
   - **Category**: "React"
   - **Difficulty**: "Easy"
   - **Questions**: Выберите созданный вопрос
4. Нажмите "Save"

## 🔍 Диагностика проблем

### Если тест не сохраняется:

1. **Проверьте консоль браузера** (F12 → Console)
   - Хук `debugTestCreation` покажет подробную информацию
   - Ищите ошибки валидации

2. **Проверьте логи сервера**
   - В терминале где запущен `pnpm dev`
   - Ищите ошибки базы данных

3. **Проверьте аутентификацию**
   - Убедитесь, что вы залогинены в админке
   - Попробуйте перелогиниться

### Возможные ошибки:

#### "Test must have at least one question"

- **Решение**: Добавьте хотя бы один вопрос к тесту

#### "Question must have at least 2 options"

- **Решение**: Добавьте минимум 2 варианта ответа

#### "Question must have at least one correct answer"

- **Решение**: Отметьте хотя бы один вариант как правильный

#### "All options must have text"

- **Решение**: Заполните текст для всех вариантов ответа

## 🚀 После успешного тестирования

### Восстановите правильный доступ:

```typescript
// В Tests/index.ts и Questions/index.ts
access: {
  create: ({ req: { user } }) => Boolean(user), // Только аутентифицированные
}
```

### Удалите отладочные хуки:

- Удалите `debugTestCreation` из `Tests/hooks/`
- Уберите его из `beforeChange` в конфигурации

## 📝 Результат

После выполнения этих шагов вы должны быть able to:

- ✅ Создавать вопросы в админке
- ✅ Создавать тесты в админке
- ✅ Связывать вопросы с тестами
- ✅ Сохранять все изменения

---

**Статус**: 🔧 Диагностика настроена  
**Следующий шаг**: Тестирование в админке  
**Команда**: `pnpm run create-admin` → `pnpm dev` → тестирование
